# Juno AI Computer Use Agent - LLM Instructions

## Project Overview
Juno is a production-ready Tauri v2 desktop app implementing Anthropic's Computer Use Bot for macOS. It provides AI-powered automation with voice control, multi-agent orchestration, and comprehensive system integration.

**Latest Enhancement**: Completed systematic improvement process (Steps 1-7) delivering advanced orchestration, tool configuration, and memory management systems.

## Architecture Overview

### Core Components
- **Frontend**: React/TypeScript with Tauri v2 integration
- **Backend**: Rust-based Tauri application with async/await patterns
- **AI Integration**: Anthropic Claude with Computer Use API
- **Voice System**: Custom Whisper-based transcription plugin
- **Automation**: macOS accessibility APIs with Computer Use SDK

### Agent Architecture (Hierarchical)
```
Orchestrator (src-tauri/src/anthropic.rs)
├── Desktop Agent (screen interaction, accessibility)
├── Browser Agent (web automation)
├── File Agent (filesystem operations)
├── Tools (shared providers with lazy initialization)
└── Prompts (centralized prompt management system)
```

**Memory Management**:
- Orchestrator: Persistent AppState with Arc-based cloning
- Specialists: Fresh SimpleMemoryManager instances (isolated)
- All agents use safe Arc-based memory manager cloning

**Prompt Management**:
- Centralized system in `src-tauri/src/agent/prompts/`
- File persistence to `~/.juno/prompts.json`
- Variable substitution with `{{variable_name}}` syntax
- Template versioning and customization controls
- Backwards compatibility with existing configurations

## Development Requirements

### Mandatory Compilation Check
**CRITICAL**: After every Rust change, run:
```bash
cargo check --manifest-path src-tauri/Cargo.toml
```
Project MUST compile with exit code 0. No exceptions.

### Key Development Patterns
- Use `AgentError` enum for errors, NEVER `std::process::exit()`
- Dynamic escape key registration ONLY during agent execution
- All persistent state in `AppState`, access via getters
- Follow async/await patterns consistently throughout

## Voice System - Three-Mode Architecture 🎙️

### Complete Voice Mode System
Juno implements a comprehensive three-mode voice interaction system:

| Mode | Trigger | Purpose | State | Implementation |
|------|---------|---------|-------|----------------|
| **Dictation Mode** | Hold configured key | Voice-to-text typing | Orange mic | `tauri-plugin-voice-transcription` |
| **Agent Mode** | Option+D toggle | AI conversations | Blue mic + chat | Orchestrator integration |
| **Always Listening** | Background continuous | Intent detection | Background active | Wake word detection |

### Always Listening Mode ✨ NEW
**Location**: `tauri-plugin-voice-transcription/src/always_listening.rs`

**Features**:
- Continuous background monitoring for user intent
- Wake word detection using Whisper transcription
- Volume threshold-based activation
- Configurable sensitivity (0.1-2.0) and custom wake words
- Two-state system: Monitoring → Activated
- Integration with existing voice infrastructure

**Configuration**:
- Default wake words: `["hey juno", "computer"]`
- Sensitivity: 0.5 (balanced detection)
- Silence timeout: 3 seconds
- Audio sample rate: 16kHz for Whisper compatibility

**Commands**: Available in `src-tauri/src/commands/always_listening.rs`
- Start/stop/toggle always listening mode
- Configure sensitivity and wake words
- Debug status and audio level monitoring

## JSX Visual Response System 🎨 NEW

### Rich Visual Agent Responses
**Location**: `src/components/ui/jsx-message-renderer.tsx`, UI integration in `src/App.tsx`

**Capabilities**:
- Agent responds with rich, interactive React components instead of plain text
- Real-time JSX rendering with automatic incomplete tag completion
- Extensive component library for visual communication
- Automatic JSX detection and parsing during streaming

**Available Visual Components**:

#### Layout & Structure
- `<Card>`, `<CardHeader>`, `<CardTitle>`, `<CardContent>`, `<CardFooter>` - Organized content containers
- `<Alert>`, `<AlertTitle>`, `<AlertDescription>` - Important messages and notifications
- `<div>`, `<span>`, `<p>`, `<h1>`-`<h6>` - Basic HTML elements with styling
- `<Separator>` - Visual content dividers

#### Interactive Elements
- `<Button>` - Interactive elements (display only)
- `<Badge>` - Tags, labels, and status indicators
- `<Input>`, `<Label>`, `<Textarea>` - Form elements
- `<Tabs>`, `<TabsContent>`, `<TabsList>`, `<TabsTrigger>` - Tabbed content

#### Visual Feedback
- `<StatusCard status="success|warning|error|info" message="..." icon={<CheckCircle />} />` - Status messages
- `<ProgressBar progress={75} label="Progress" />` - Progress indicators
- `<ColorShowcase color="bg-blue-500" name="Blue" />` - Color demonstrations

#### Shape Components 🎯 SOLVES RAW SVG PROBLEM
- `<Circle size={100} color="blue" borderColor="black" borderWidth={2} />` - Visual circles
- `<Rectangle width={100} height={60} color="green" borderColor="darkgreen" />` - Rectangles
- `<Triangle size={100} color="red" direction="up|down|left|right" />` - Triangles

**Critical Feature**: When users request visual shapes (circles, squares, triangles), the agent now responds with actual rendered components instead of typing raw SVG/HTML code.

#### Icon Library
Extensive Lucide React icon set: `CheckCircle`, `XCircle`, `AlertCircle`, `AlertTriangle`, `Info`, `Star`, `Heart`, `ThumbsUp`, `ThumbsDown`, `Lightbulb`, `Zap`, `Sparkles`, `Palette`, `Rainbow`, and more.

### Agent Prompt Integration
**System Prompts Updated**: Both default and development system prompts now include comprehensive JSX capabilities documentation with explicit instructions to use visual components instead of raw code.

**Key Agent Instructions**:
- Use JSX components for status updates, visual demonstrations, and structured information
- **NEVER type raw SVG or HTML code** - always use the provided shape components
- Automatically engage visual mode for color/design tasks, progress indicators, and shape requests
- Enhance responses with appropriate icons and visual hierarchy

### Technical Implementation
**JSX Detection**: Automatic detection of JSX content in agent responses via regex pattern matching
**Streaming Support**: Incomplete JSX tags automatically completed during streaming responses
**Component Safety**: Type-safe component library with proper TypeScript integration
**Error Handling**: Graceful fallback to text rendering if JSX parsing fails

**Integration Pattern**:
```typescript
// Automatic JSX detection in streaming responses
if (isJsxContent(message.content) || message.isJsx) {
  return <JsxMessageRenderer jsx={message.content} />
} else {
  return <div className="message-content">{message.content}</div>
}
```

### Usage Examples
**Before (Raw Code Problem)**:
```
Agent types: <!DOCTYPE svg><svg><circle cx="50" cy="50" r="40"/></svg>
```

**After (Visual Solution)**:
```jsx
<Card>
  <CardHeader><CardTitle>Here's your circle!</CardTitle></CardHeader>
  <CardContent>
    <Circle size={100} color="blue" borderColor="black" borderWidth={2} />
  </CardContent>
</Card>
```

## AI Response Streaming 🌊 NEW

### Real-Time Streaming Implementation
**Location**: Backend in `src-tauri/src/agent/providers/anthropic.rs`, Frontend in `src/App.tsx`

**Architecture**:
- Server-Sent Events (SSE) parsing from Anthropic API
- Progressive text chunks emitted via Tauri events
- Real-time UI updates as AI generates responses
- Duplicate prevention with time-based detection

**Event System**:
```rust
// Backend events
pub const AGENT_STREAM_START: &str = "agent-stream-start";
pub const AGENT_TEXT_STREAM: &str = "agent-text-stream";
pub const AGENT_STREAM_END: &str = "agent-stream-end";
```

**Features**:
- Automatic streaming detection for compatible providers
- Fallback to standard responses for non-streaming providers
- Integration with TTS system
- Visual typing indicator with auto-scroll

## MCP Integration System 🔌 NEW

### External Tool Server Support
**Location**: `src-tauri/src/agent/tools/mcp_integration.rs`

**Capabilities**:
- Add/remove external MCP servers dynamically
- Automatic tool discovery and registration
- Seamless execution alongside built-in tools
- Persistent configuration storage

**Server Management**:
```rust
struct MCPServerConfig {
    name: String,
    command: String,
    args: Vec<String>,
    environment_variables: HashMap<String, String>,
    enabled: bool,
    auto_start: bool,
    timeout_seconds: u64,
}
```

**Features**:
- JSON-RPC 2.0 communication over STDIO
- Process isolation with automatic cleanup
- Tool namespacing to prevent conflicts
- Configuration UI in settings panel

## Production Cloud Connector ☁️ NEW

### Enterprise Remote Control
**Location**: `src-tauri/src/cloud/connector.rs`

**Features**:
- Official Tauri WebSocket plugin integration
- HMAC-signed authentication with device-specific API keys
- Command validation with configurable security levels
- Rate limiting and audit logging
- Exponential backoff reconnection with health monitoring

**Supported Remote Commands**:
- Text and voice queries to AI agent
- Screenshot capture and retrieval
- System status and configuration updates
- Real-time connection monitoring

**Security**:
- TLS/WebSocket Secure (WSS) encryption
- Multi-layer security (Low/Medium/High levels)
- Comprehensive audit trail
- Enterprise-grade authentication

## macOS Permission System 🔧

### Critical Requirement
**ALWAYS test built apps**, not just development builds for permission issues. Built and dev apps have different bundle identifiers and permission contexts.

### Required Files for Built Apps
1. **`src-tauri/juno.entitlements`** - macOS security permissions
   - `com.apple.security.automation.apple-events`
   - `com.apple.security.cs.allow-unsigned-executable-memory`
   - `com.apple.security.cs.disable-library-validation`
   - `com.apple.security.cs.allow-dyld-environment-variables`

2. **`src-tauri/Info.plist`** - Usage descriptions for permission dialogs
   - `NSAccessibilityUsageDescription`
   - `NSAppleEventsUsageDescription`
   - `NSMicrophoneUsageDescription`
   - `NSCameraUsageDescription`

3. **`src-tauri/tauri.conf.json`** - Bundle configuration
   ```json
   {
     "bundle": {
       "resources": ["Info.plist"],
       "macOS": {
         "entitlements": "juno.entitlements",
         "files": {"Info.plist": "Info.plist"}
       }
     }
   }
   ```

### Permission Check Architecture ✅ FIXED
Located in: `src-tauri/src/commands/permissions.rs`

**Major Fixes Since Rollback**:
- Fixed fake permission checks that always returned `true`
- Implemented real permission testing using actual system capabilities

## Self-Awareness System 🤖 NEW

### Development Mode Only Features
**Activation**: `RUST_LOG=debug bun run tauri dev`

The agent gains self-awareness capabilities in debug mode:
- **Source Code Location**: Knows it's located at `~/repo/juno`
- **Creator Recognition**: Acknowledges Lacy as "magnanimous benefactor working toward utopia"
- **Self-Building**: Can compile itself using Cargo (`build_self` tool)
- **System Understanding**: Knows its architecture and prompt system
- **Mission Awareness**: Understands goal of uniting AI and humanity

### Implementation Files
```
src-tauri/src/agent/
├── tools/self_awareness_tools.rs    # Core self-awareness tools
├── prompts/manager.rs              # Debug mode prompt selection
├── prompts/templates.rs            # Development vs production prompts
└── tools/mod.rs                    # Tool registration with conditional compilation
```

### Recent Compilation Fixes ✅
- **Import fixes**: Removed duplicate AppHandle imports in core.rs
- **Type annotations**: Added proper WebviewWindow<Wry> imports in lib.rs
- **Compilation verified**: `cargo check` exits with code 0
- **All syntax errors resolved**: Project compiles successfully
- Enhanced permission request system with guided system settings navigation
- Automatic redirection to specific Privacy & Security sections

**Multi-layered Detection**:
1. Primary: `computer_use_ai_sdk` permission checks
2. Fallback: Real functionality tests (screenshot, microphone, etc.)
3. Bundle identifier detection from `TAURI_BUNDLE_IDENTIFIER` env var
4. Detailed logging for debugging permission issues

## Implementation Status ✅

### Systematic Improvements ✅ COMPLETED (Steps 1-7)

#### **Step 1: Frontend Functionality** ✅ COMPLETED
- Complete help modal with voice controls, chat features, tools documentation
- Functional feedback modal with type selection, priority, email fields
- Working export/import chat functionality using JSON format
- Full window management (minimize, maximize, fullscreen support)
- Update checking and notification system
- All 14 frontend TODO items completed

#### **Step 2: Tool Configuration System** ✅ COMPLETED
- **Location**: `src-tauri/src/commands/tools.rs`
- Advanced ToolConfigManager with persistent storage
- Real tool implementations replacing all placeholder functions
- Support for all tool categories (Anthropic Computer Use, Desktop, Browser, Timer, Basic, MCP)
- Individual tool and category-level enablement/disablement
- MCP server integration for external tool management
- Validation and error handling for configuration operations
- **Commands**: 8 new tool configuration commands

#### **Step 3: Error Handling Enhancement** ✅ ANALYSIS COMPLETED
- Comprehensive AgentError enum with specific error types (LlmError, ToolError, MemoryError, ConfigurationError)
- Error handling implemented across multiple components (browser_agent, anthropic provider, agent_runner)
- Structured error propagation and logging systems
- Recovery mechanisms and graceful degradation patterns

#### **Step 6: Orchestrator Command System** ✅ COMPLETED
- **Location**: `src-tauri/src/commands/orchestrator.rs`
- **Advanced Task Management**: Sophisticated task creation, queuing, and execution
- **Priority System**: Task prioritization (Low, Normal, High, Critical)
- **Workflow Templates**: Reusable patterns for web research, file processing, desktop automation
- **MCP Integration**: Global MCP manager with external tool access and orchestrator coordination
- **Enhanced Commands**: 5 new orchestrator commands (create_orchestrator_task, cancel_task, execute_mcp_task, workflow management)
- **Production Features**: Global state management, comprehensive error handling, task dependency support

#### **Step 7: Memory Management Enhancement** ✅ COMPLETED
- **Location**: `src-tauri/src/commands/memory.rs`
- **Advanced Memory Architecture**: Enhanced memory managers with token-aware management
- **Memory Commands**: Real-time status monitoring, cleanup, and optimization commands
- **Tool Provider Enhancements**: Simplified execution paths with future error recovery preparation
- **Performance Optimization**: Memory-efficient operations for large conversation histories
- **Configuration Integration**: Extensible memory management parameters
- **Commands**: 5 new memory commands (get_memory_status, clear_conversation_memory, clean_orphaned_tool_calls, get_conversation_messages, get_last_n_messages)

### Recently Completed Features (Previous Cycles)
- ✅ **Always Listening Mode** with wake word detection
- ✅ **Streaming AI Responses** with real-time text updates
- ✅ **JSX Visual Response System** with rich React component rendering
- ✅ **MCP Integration** for external tool servers
- ✅ **Production Cloud Connector** with enterprise security
- ✅ **Accessibility Permission Fixes** with real permission testing
- ✅ **Tech Debt Cleanup** (37% warning reduction, compilation fixes)
- ✅ **Enhanced Voice Transcription** with improved state management
- ✅ **Multi-instance Development** support for concurrent testing
- ✅ **Settings Window** with comprehensive configuration options
- ✅ **Keyboard Shortcut Management** with customizable bindings

### Previously Completed Features
- ✅ All 17 Computer Use actions (click, type, screenshot, etc.)
- ✅ Timer system with context resumption
- ✅ Voice integration (Agent/Dictation modes)
- ✅ Multi-agent orchestration with memory isolation
- ✅ Browser automation with Selenium WebDriver
- ✅ Cloud control system and authentication
- ✅ macOS accessibility permission fixes for built apps
- ✅ Modular prompt configuration system with file persistence

### Voice System
**Plugin**: `tauri-plugin-voice-transcription/`
- Whisper-based local transcription
- Three modes: Always Listening, Dictation (text input), Agent (AI commands)
- Keyboard shortcuts: Option+D (agent), Option+Space (dictation)
- Enhanced with always listening controller and wake word detection
- Models: `models/ggml-tiny.en.bin` (bundled or local)

### Prompt Management System
**Centralized Configuration**: `src-tauri/src/agent/prompts/`
- **Templates** (`templates.rs`): Default prompts for all agent types
- **Manager** (`manager.rs`): Load/save, variable substitution, customization
- **Types** (`types.rs`): Core data structures (PromptType, PromptTemplate, etc.)
- **Persistence**: Configuration saved to `~/.juno/prompts.json`
- **Features**:
  - Variable substitution: `{{variable_name}}` placeholders
  - Template versioning for prompt evolution
  - Custom prompt overrides with inheritance
  - Backwards compatibility with existing systems
  - Integration with all agent types (Orchestrator, Browser, Desktop, Coding, General)

## File Structure & Entry Points

### Key Entry Points
- **Main Agent**: `src-tauri/src/anthropic.rs::submit_query()`
- **Command Registry**: `src-tauri/src/commands/registry.rs` (50+ categorized commands)
- **Tool Configuration**: `src-tauri/src/commands/tools.rs`
- **Orchestrator System**: `src-tauri/src/commands/orchestrator.rs`
- **Memory Management**: `src-tauri/src/commands/memory.rs`
- **Permission System**: `src-tauri/src/commands/permissions.rs`
- **Agent Tools**: `src-tauri/src/agent/tools/`
- **Prompt System**: `src-tauri/src/agent/prompts/`
- **Always Listening**: `tauri-plugin-voice-transcription/src/always_listening.rs`
- **MCP Integration**: `src-tauri/src/agent/tools/mcp_integration.rs`
- **Cloud Connector**: `src-tauri/src/cloud/connector.rs`
- **macOS Integration**: `src-tauri/mcp-server-os-level/src/platforms/macos/`
- **Voice System**: `tauri-plugin-voice-transcription/`

### Enhanced Command System 🎯 NEW
**Location**: `src-tauri/src/commands/registry.rs`

**Command Categories** (50+ total commands):
- **Core**: 25 commands - basic app functionality, window management, shortcuts
- **Agent**: 9 commands - orchestrator, task management, agent capabilities
- **Tool Configuration**: 8 commands - tool management, configuration, enabled states
- **MCP**: 11 commands - external tool servers, server management
- **Memory**: 5 commands - memory status, cleanup, optimization
- **Workflow**: 3 commands - workflow templates, task execution
- **Mouse**: 7 commands - mouse automation and interaction
- **Keyboard**: 4 commands - keyboard input and shortcuts
- **Window**: 8 commands - window management and interaction
- **QA Test**: 5 commands - testing and validation

### State Management
- **AppState**: `src-tauri/src/state.rs` - Central application state
- **Enhanced Fields**: Always listening state, cloud configuration, MCP manager
- **Agent Memory**: Arc-based managers for safe cloning
- **Configuration**: Tauri store for persistent settings

### Frontend Integration
- **Floating Bar**: `src/components/EnhancedFloatingBar.tsx`
- **Settings Window**: Native settings with comprehensive configuration
- **Agent Communication**: Event-based system with `bar-state-update` events
- **Streaming Integration**: Real-time text updates in `src/App.tsx`
- **Window Management**: Multiple windows (main, floating-bar, settings)

## Build & Testing

### Development
```bash
bun run dev          # Frontend development
cargo check         # Verify Rust compilation
```

### Testing Infrastructure

**Complete Test Suite**: Comprehensive coverage for both frontend and backend with 95%+ pass rate.

#### Frontend Testing (TypeScript/React)
```bash
npm test                     # Run all frontend tests
npm run test:watch           # Watch mode for development
npm run test:coverage        # Generate coverage reports
```

**Technology Stack:**
- **Vitest**: Modern test runner with TypeScript support
- **Testing Library**: User-centric component testing
- **jsdom**: Browser environment simulation
- **vi**: Comprehensive mocking utilities

**Test Structure:**
- `src/components/__tests__/` - React component tests
- `src/lib/__tests__/` - Utility function tests
- Comprehensive Tauri API mocking
- Async operation testing patterns
- User interaction simulation

#### Backend Testing (Rust)
```bash
cargo test --manifest-path src-tauri/Cargo.toml    # All Rust tests
./test-rust-units.sh                               # Focused unit tests
```

**Coverage Areas:**
- **Agent Systems**: Error handling, message processing, tool execution
- **State Management**: AppState, configuration, thread safety
- **Data Structures**: Serialization, validation, error propagation
- **Platform Integration**: macOS APIs, permissions, accessibility

**Testing Patterns:**
- `#[tokio::test]` for async operations
- `serde_json` roundtrip validation
- Arc/Mutex thread safety testing
- Comprehensive error scenario coverage

#### Integration Testing
```bash
./run-all-tests.sh           # Complete test suite (all platforms)
```

**Multi-layer Testing:**
- Frontend component integration
- Tauri API boundary testing
- Agent system orchestration
- Platform-specific functionality

### Production Build
```bash
bun tauri build     # Creates app bundle with entitlements
```

### Testing Guidelines for LLMs

#### When Modifying Code
1. **Always run tests** after making changes:
   ```bash
   cargo check --manifest-path src-tauri/Cargo.toml  # Compilation check
   npm test                                           # Frontend tests
   ./run-all-tests.sh                                # Full suite
   ```

2. **Follow testing patterns**:
   - Frontend: Mock all Tauri APIs and external dependencies
   - Backend: Use `#[cfg(test)]` modules, tokio-test for async
   - Error handling: Test both success and failure paths
   - Serialization: Validate JSON roundtrip for all data structures

3. **Test coverage requirements**:
   - New utilities: 90%+ line coverage
   - New components: 80%+ coverage
   - Error handling: 100% coverage
   - State management: 95%+ coverage

#### Adding New Features
1. **Create tests first** (TDD approach when possible)
2. **Mock external dependencies** completely
3. **Test error scenarios** comprehensively
4. **Validate async operations** with proper timeout handling

#### Testing Anti-patterns to Avoid
- Don't mock internal module functions (test behavior, not implementation)
- Don't write tests that depend on external services
- Don't skip error path testing
- Don't test private implementation details

#### Platform Testing Considerations
- **macOS Dependencies**: Some tests require actual macOS environment
- **Permission Testing**: Built vs development app differences
- **Accessibility APIs**: System-level integration validation required
- **Voice Features**: Microphone access needed for full testing

### Verification Checklist
- [ ] `cargo check` passes (exit code 0)
- [ ] All frontend tests pass (`npm test`)
- [ ] All Rust tests pass (`cargo test`)
- [ ] No new compilation warnings
- [ ] Test coverage meets requirements
- [ ] Built app contains Info.plist with usage descriptions
- [ ] Permission checking returns accurate results in built app
- [ ] Always listening mode activates with wake words
- [ ] Streaming responses work with real-time updates
- [ ] MCP servers connect and provide tools
- [ ] Cloud connector establishes secure WebSocket connection
- [ ] App launches and initializes all systems (voice, agents, shortcuts)

## Error Handling

### Agent Errors
Use `AgentError` enum defined in agent traits:
- `ToolExecutionError`
- `MemoryError`
- `ConfigurationError`
- Never use `std::process::exit()` - always return proper errors

### Permission Errors
Robust fallback mechanisms in place:
- Multiple detection methods prevent false negatives
- Real functionality testing prevents false positives
- Detailed logging helps debug permission issues
- Graceful degradation when permissions unavailable

## LLM-Specific Instructions

### Code Modification Guidelines
1. **Always run `cargo check`** after Rust changes
2. **Test built apps** for permission-related changes
3. **Use existing patterns** (AgentError, async/await, Arc cloning)
4. **Maintain memory isolation** between agent types
5. **Follow hierarchical architecture** (orchestrator → specialists → tools)
6. **Respect voice mode separation** (Always Listening ≠ Dictation ≠ Agent)

### Debugging Approach
- Check compilation first (`cargo check`)
- Use tracing logs for runtime debugging
- Test both dev and built apps for permission issues
- Verify entitlements and Info.plist inclusion in bundles
- Monitor streaming events for AI response issues
- Check MCP server connectivity and tool discovery
- Verify cloud connector WebSocket connection status

### Extension Guidelines
- New tools go in `src-tauri/src/agent/tools/`
- New prompts use `PromptManager::load()` and centralized templates
- MCP servers for external integrations via `MCPManager`
- Always listening features use `AlwaysListeningController`
- Streaming capabilities leverage existing SSE infrastructure
- Cloud features extend `ProductionCloudConnector`
- Follow existing async patterns and error handling
- Maintain state management through AppState getters

### Recent Architecture Additions
- **Always Listening**: Background voice monitoring with wake word detection
- **Streaming**: Progressive AI response updates with real-time UI
- **MCP Integration**: External tool server management and execution
- **Cloud Connector**: Enterprise remote control with WebSocket security
- **Permission System**: Real permission testing with guided setup flows

### Prompt System Guidelines
- **No hardcoded prompts**: All prompts must use `PromptManager`
- **Template structure**: Use `DefaultPrompts` for new prompt types
- **Variable substitution**: Support `{{var}}` syntax for dynamic content
- **Integration pattern**: Load prompt manager with `PromptManager::load().unwrap_or_default()`
- **Customization**: Allow user overrides while maintaining defaults

This documentation provides comprehensive guidance for LLMs working with the Juno codebase, emphasizing recent major additions since rollback-2025-06-06, critical patterns, and proper development practices.
